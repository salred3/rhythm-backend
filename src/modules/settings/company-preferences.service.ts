export class CompanyPreferencesService {
  async getMatrixWeights(companyId: string): Promise<{ impactWeight: number; effortWeight: number; timelineWeight: number } | null> {
    return null; // placeholder
  }
}
